#!/bin/sh

echo "stop all processes ..."
jps | grep sbt-launch | awk '{print $1}' | xargs kill -9

log=$1
if [ "x" = "x$log" ]; then
  log="/tmp/psrs"
fi

if [ -d $log ]; then
  echo "remove psrs directory: $log"
  rm -rf $log
fi
