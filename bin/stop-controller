#!/bin/sh

echo "stop controller processes ..."
jps | grep sbt-launch | awk '{print $1}' | xargs kill -9

log=`ls /tmp/psrs/controller_*.log`
if [ "x" != "x$log" ]; then
  echo "remove controller log file: $log ..."
  rm $log
fi
